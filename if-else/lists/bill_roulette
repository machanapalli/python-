#this is the input command for the list 
names = names_string.split(",")

import random
no_of_people = len(names)
#list start with 0 so the last person in list will be no_of people-1
random_number= random.randint(0,no_of_people-1)
bill_payer = names[random_number]
print(f"{bill_payer} will pay the bill.")
